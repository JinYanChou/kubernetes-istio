apiVersion: batch/v1
kind: Job
metadata:
  name: redis-cluster-job
spec:
  ttlSecondsAfterFinished: 0
  template:
    metadata:
      name: redis-cluster-job
    spec:
      initContainers:
      - name: init-redis
        image: busybox:1.27.2
        command: ['sh', '-c', 'until nslookup redis-app-5.redis-service.csr.svc.cluster.local; do echo waiting for redis; sleep 5; done;']
      containers:
      - name: redis
        image: redis:5.0.6-alpine
        command:
        - sh
        - -c
        - |
          redis-cli --cluster create --cluster-replicas 1 --cluster-yes \
          `echo $(getent hosts redis-app-0.redis-service.csr.svc.cluster.local | cut -d' ' -f1)`:6379 \
          `echo $(getent hosts redis-app-1.redis-service.csr.svc.cluster.local | cut -d' ' -f1)`:6379 \
          `echo $(getent hosts redis-app-2.redis-service.csr.svc.cluster.local | cut -d' ' -f1)`:6379 \
          `echo $(getent hosts redis-app-3.redis-service.csr.svc.cluster.local | cut -d' ' -f1)`:6379 \
          `echo $(getent hosts redis-app-4.redis-service.csr.svc.cluster.local | cut -d' ' -f1)`:6379 \
          `echo $(getent hosts redis-app-5.redis-service.csr.svc.cluster.local | cut -d' ' -f1)`:6379
      restartPolicy: Never
